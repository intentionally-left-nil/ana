[package]
name = "ana-paths"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT"
description = "Filesystem layout for ana: project-root discovery and environment paths (ana.lock, env, advisory locks)"

[dependencies]
sha2 = { workspace = true }
uv-normalize = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
