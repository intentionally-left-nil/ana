[package]
name = "ana-channels"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT"
description = "Channel identity for ana: the one normalizer (normalize_channel) and the one comparator (ChannelPolicy) every other crate defers to"

[dependencies]
rattler_conda_types = { workspace = true }
rattler_redaction = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }
url = { workspace = true }

[dev-dependencies]
walkdir = "2"
