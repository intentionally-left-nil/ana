[package]
name = "ana-channels"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "MIT"
description = "Channel allow-policy validation and channel digest for ana: which channels a project/package may declare, and the flat channel list a solve should run against"

[dependencies]
ana-matchspec-convert = { path = "../ana-matchspec-convert" }
rattler_conda_types = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }
url = { workspace = true }
